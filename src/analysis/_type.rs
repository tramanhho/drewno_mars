use crate::parser::ast::*;
use std::collections::HashMap;
use std::collections::hash_map::Entry::{Occupied, Vacant};

pub fn type_error_check(prog: Box<Program>) -> Result<(), ()> {
    
    
	Ok(())
}

pub struct TypeChecker {
	
}